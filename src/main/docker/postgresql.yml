version: '2'
services:
  specifications-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/specifications/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=specifications
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
